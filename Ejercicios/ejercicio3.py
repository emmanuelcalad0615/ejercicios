import random
#Crear un programa que genere una lista de números aleatorios y los imprima en pantalla.
n=int(input("Ingrese el tamaño de la lista"))
lista_numeros=[]

for i in range(0, n):
    lista_numeros.append(random)

print(lista_numeros)